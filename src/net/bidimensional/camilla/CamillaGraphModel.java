/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package net.bidimensional.camilla;

import com.mxgraph.model.mxCell;
import com.mxgraph.model.mxGraphModel;

public class CamillaGraphModel extends mxGraphModel {

//    @Override
//    public Object setTerminal(Object edge, Object terminal, boolean isSource) {
//        Object result = super.setTerminal(edge, terminal, isSource);
//
//        // After setting the terminal, check if the edge connects two vertices
//        Object source = getTerminal(edge, true);
//        Object target = getTerminal(edge, false);
//
//        // If both source and target are set, and either is null, set the edge color to red
//        if (source != null && target != null && (source == null || target == null)) {
//            mxCell cell = (mxCell) edge;
//            cell.setStyle("strokeColor=red");
//        }
//
//        return result;
//    }

}
